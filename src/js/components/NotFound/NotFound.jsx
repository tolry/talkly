import React from "react";

export default ({children}) => {
    return (
        <div style={{
            background: '#ff0000',
            color: '#fff'
        }}>
            Route not found!
        </div>
    );
}